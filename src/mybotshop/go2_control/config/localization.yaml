/**:
  ros__parameters:
    frequency: 30.0
    two_d_mode: true
    publish_tf: true
    # predict_to_current_time: true
    # transform_time_offset: 0.1
    odom_frame: FP_ENU0
    base_link_frame: base_link
    world_frame: FP_ENU0
    use_sim_time: false
    sensor_timeout: 0.1
    # X,  Y,   Z
    # r,  p,   y,
    # X'',Y',  Z'
    # r', p',  y'
    # X'',Y'', Z''

    odom0: /fixposition/odometry_enu_smooth
    odom0_config: [true, true, false, 
                  false, false, true,
                  false, false, false,
                  false, false, false,
                  false, false, false]
    odom0_differential: false
    odom0_queue_size: 10
    
    # imu0: /fixposition/poiimu
    # imu0_config: [false, false, false,
    #               false, false, false,
    #               false, false, false,
    #               false, false, true,
    #               true,  true,  false]
    # imu0_differential: false
    # imu0_queue_size: 10
    # imu0_remove_gravitational_acceleration: true